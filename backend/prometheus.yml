global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'hellodoc-backend'
    metrics_path: /metrics
    scheme: https

    static_configs:
      - targets: ['hellodoc-backend-project.onrender.com']

    tls_config:
      insecure_skip_verify: true
